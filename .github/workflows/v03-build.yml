name: Legacy V03 Packaging

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-v03-note:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow notice
        run: echo "V03 packaging is archived. Use the standard CI and Windows Installer workflows for current development."
