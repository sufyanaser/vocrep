name: Validate and Package V03

on:
  workflow_dispatch:

jobs:
  build-v03:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "V03 artifacts were validated and generated in run 30600684668."
